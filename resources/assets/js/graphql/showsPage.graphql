query showsPageQuery {
    shows (page:1,limit:1) {
        data {
            id
            venue
            address
            date
        },
        total,
        limit,
        page,
        from,
        to,
        last_page,
        next_page_url,
        prev_page_url
    }
}